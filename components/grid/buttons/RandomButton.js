import React from 'react'

const RandomButton = () => {
    return (
        <button 
        type='button'
        onClick={() => console.log('hello')}
        >
            Random
        </button>
    )
}

export default RandomButton